import React from 'react';

interface IProps {}
interface IState {}

const AppRoot = (props: IProps, state: IState) => {
  return (
    <section>
      <p>Hello</p>
    </section>
  );
};

export default AppRoot;
